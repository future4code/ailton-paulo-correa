type pokemon = {
  name: string;
  types: string;
  healthPoints: number;
};

const pokemon1: pokemon = {
  name: "Charmander",
  types: "Fire",
  healthPoints: 28,
};

const pokemon2: pokemon = {
  name: "Bulbasaur",
  types: "Grass/Poison",
  healthPoints: 31,
};

const pokemon3: pokemon = {
  name: "Squirtle",
  types: "Water",
  healthPoints: 35,
};

// 3. b), c) e d) para transpilar utilizamos o tsc && .build/exe4.js, assim irei transpilar todos os arquivos .ts e converter para .js que se encotraram na pasta build

console.log(
  "3. b), c) e d) para transpilar utilizamos o tsc && .build/exe4.js, assim irei transpilar todos os arquivos .ts e converter para .js que se encotraram na pasta build"
);
